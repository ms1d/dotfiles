# NEOVIM CONFIGURATION

## NOTE FOR WINDOWS MACHINES

You **must** modify `treesitter.config` to `treesitter.configs` in the respective
area where the plugin is loaded in plugins.lua since windows has a different name
